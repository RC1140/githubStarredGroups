The github star grouper is a little app
can be used to group your stared into more
manageable groups.

Requirements :

Flask
Redis
Python

TODO :

Add export feature
Add import feature
Consider hosted version

Setup:

clone the repo locally (git clone git://github.com/RC1140/githubStaredGroups.git)

install the requirements with pip (pip install -r requirements.txt)

install redis (use package manager of choice)

run app with  : ( python web.py )

access the server on localhost:8000
